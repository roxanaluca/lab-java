# Grafana Datasources Configuration
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9096
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "15s"
      httpMethod: POST
